Templates for testing.  The `base` file doesn't include the actual
keyservers; just everything else.  To generate the output files, we
substitute in both files, replacing `%...%` strings with real values.
